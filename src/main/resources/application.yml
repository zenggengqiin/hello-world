spring:
  datasource:
    type: com.zaxxer.hikari.HikariDataSource
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/ck?useUnicode=true&useSSL=false&characterEncoding=utf8
    username: root
    password: root

mybatis-plus:
  mapper-locations: classpath*:/mapper/*.xml
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
    devtools:
      restart:
        enabled: true
        additional-paths: src/main
        exclude: static/**
server:
  port: 8008


